---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h1">;

const { ...attrs } = Astro.props;
---

<h1
  class:list={[
    "mb-6 text-3xl leading-tight font-bold text-balance text-gray-900 lg:text-4xl",
    attrs.class,
  ]}
  {...attrs}
>
  <slot />
</h1>
